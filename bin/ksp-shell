#!/bin/bash

rc=`tempfile -p kspsh`
trap "rm -f '$rc'" EXIT

baserc="$HOME/.re.pl/repl.rc"

[ -f "$baserc" ] && cp "$baserc" "$rc"
cat <<ENDRC >> $rc
use utf8;
use Math::Trig;
use Math::Vector::Real;
use KSP qw(:all);
ENDRC

re.pl --rcfile "$rc"

